# to sum up:
# 1.spreadsheet have features that doesn't in Csv file:
# lik:
# font syles 
# -height-width with cells
# merged and unmerged cells
# charts and images 
# multiple worksheets
# 2 csv.reader(fileobj),csv.writer(fileobj)
# 3.we use 'r','w' modes to open files into csv.reader or csv.writer
# 4.writer.writerow(put a list to write into csv)
# 5.delimeter -> used to separete cells in csv
# lineterminator -> used to add (newline or tab at the end of each row)
# 6.json.loads(stringofjson)-> return a paython data structure (dictionary)
# 7.json.dumps(python data structure) -> return a json data.