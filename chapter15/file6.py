#To sum Up:
# Unix epoch is a fixed time we reference to (1 January 1970)
# time.time() return the number of second since unix epoch
# round() return a rounded number 
# datetime() return a datetime object
# timedelta() return a delta object (duration of the time)
# to make thread to run function called spam:
# threading.thread(target=spam)
# to avoid concurrancy use local variables with each thread
# to run any program you can use subprocess module
# import subprocess
# subprocess.Popen('C:\Windows\System32\calc.exe')
